// Variables 
var mongoose = require('mongoose'); // pour pouvoir utiliser mongoose
var joueur=[username, password];
var résultats=[date, player1, player2, winner];
var date = Date("<YYYY-mm-dd>");
var player1=[userename]; // pseudo du 1er joueur
var player2=[userename]; // pseudo du 2eme joueur
var winner=[username]; // pseudo du vainqueur

// Fonction recherche username & password 
db.joueur.find( { username : pseudo, password : password } )
if(!(db.joueur.find( { username : pseudo } ))){
    db.joueur.insert({ name: username, password: password})
}

//Fonction ajout des résultats 
db.résultats.insert({date: date, player1: player1, player2: player2, winner:winner})
